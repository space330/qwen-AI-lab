# Qwen Agent Lab V1.2 验收记录

日期：2026-06-04

## 自动化验证

- `npm test`：覆盖上下文顺序与裁剪、摘要批次、仓库 CRUD、级联删除、旧数据迁移、120 条消息顺序、CSV、图表数据抽取和轻量 localStorage。
- `node --check`：覆盖前端控制器、渲染器、IndexedDB 仓库和后端上下文相关模块。
- `git diff --check`：无格式错误。

## 运行态验证

- `/api/health` 返回正常，API Key 配置状态只由后端报告。
- `/api/qwen/plan` 接受上下文并过滤不允许的历史角色。
- 真实 Qwen 调用能使用此前摘要与历史回答正确回答追问。
- `POST /api/qwen/conversation-summary` 返回纯文本摘要。
- 浏览器验证会话抽屉、个人档案、模式独立会话、自动标题、流式状态、最终回答和刷新恢复。
- 浏览器控制台无错误。

## 验收项状态

1. 多轮追问理解：通过。
2. 会话上下文隔离：通过自动化和模式切换验证。
3. 不同模式独立会话：通过。
4. 刷新后恢复会话、消息和档案：通过；附件恢复由仓库测试覆盖。
5. 会话 CRUD 与搜索：通过代码、仓库测试和浏览器入口验证。
6. 长会话只发送摘要与最近 8 轮：通过自动化测试。
7. 摘要失败不阻塞对话：通过非阻塞实现和错误降级检查。
8. 本地用户入口：通过浏览器验证。
9. 数据导出和清除：实现完成，清除操作需产品验收时人工确认。
10. 100 条以上消息：120 条消息仓库测试通过；仍建议人工进行滚动体验验收。
11. API Key 只存在后端：前端与文档扫描未发现密钥。
12. 连续使用 30 分钟：需要人工时长验收。

## 尚需人工验收

- 连续使用 30 分钟的稳定性和数据完整性。
- 100 条以上真实消息的滚动、输入和切换手感。
- 删除、清空全部数据等破坏性操作的最终产品确认。
